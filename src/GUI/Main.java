package GUI;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.List;
import java.util.Random;
import java.util.stream.Collectors;

public class Main {
    public static void main(String[] args) {
        //Moh m=new Moh();
        //Bn bn=new Bn();
        //Ev n=new Ev();
        m_e m=new m_e();
        /*Main_Form m=new Main_Form();
        Random random=new Random();
        random.ints().limit(10).forEach(System.out::println);
        List<Integer> numbers= Arrays.asList(2,1,5,7,2,3,1);
        List<Integer> squareList=numbers.stream().map(i ->i*i ).distinct().collect(Collectors.toList());
        List<Integer> squareList1=numbers.stream().map(i ->i*i ).collect(Collectors.toList());
        squareList1.forEach(System.out::println);
        List<String> strings=Arrays.asList("ab","bcd","","acv","");
        long x=strings.stream().filter(s -> !s.isEmpty()).count();
        System.out.println(strings.parallelStream().filter(s -> !s.isEmpty()).collect(Collectors.joining(",")));
        System.out.println(x);
        long y=strings.stream().filter(String::isEmpty).count();
        System.out.println(y);
        random.ints().limit(10).sorted().forEach(System.out::println);*/
    }
}
